// method chaining

let userName = window.prompt("enter your username")

userName = userName.trim();
let letter = userName.charAt(0).toUpperCase();
let remainLetter = userName.slice(1).toLowerCase();
let finalName = ley


document.getElementById("h2").textContent = finalName;

userName = userName.trim().charAt(0).toUpperCase() + userName.trim().slice(1).toLowerCase();


// && AND
// || OR
// ! NOT
// = assignment operator 
// == equals to
// === strict equals to (values and datatypes)
// != not equals to
// !== strict not equals to